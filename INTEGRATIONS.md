# 통합 카탈로그 (INTEGRATIONS)

> 이 SDK·API 위에 만들어진 봇·대시보드·도구의 공식 목록. 풀문 생태계의
> "플러그인 마켓"이다. 자기 화폐를 만들지 않고 원장을 같이 읽는 프로젝트라면
> 누구나 PR 한 장으로 등록할 수 있다.

## 등록 자격 — 3가지 철칙 준수

등록 자체는 자유지만, 유지되려면 아래를 지켜야 한다. 운영자는 등록된 통합이
이 규칙을 깨면 `verified`를 빼거나 목록에서 내린다.

1. **잔액 캐시 금지** — 표시 직전에 조회한다.
2. **자체 화폐/잔액 테이블 금지** — 읽기 API만 쓴다. 쓰기 엔드포인트는 존재하지
   않는다(405).
3. **숫자 하드코딩 금지** — 배수·캡은 `/v1/config`에서 읽는다.

매너 항목: 폴링 주기 5초 이상, `429`를 받으면 백오프, 트래픽 폭주 금지.

## 등록 절차

1. 이 레포를 포크한다.
2. [`registry/integrations.json`](./registry/integrations.json)에 항목을 추가한다.
   스키마는 아래와 같다.
3. `npm run validate:registry`가 통과하는지 확인한다. CI도 같은 검증을 돌린다.
4. PR을 보낸다. 운영자가 확인 후 병합한다.

### 항목 스키마

```json
{
  "name": "내 봇 이름",
  "type": "bot | dashboard | tool | client",
  "description": "한 줄 소개 — 무엇을 보여주는지 (40자 내외)",
  "url": "https://초대링크-또는-사이트-또는-레포",
  "author": "제작자 핸들",
  "addedAt": "2026-09-08",
  "verified": false
}
```

- `type`: `bot`(디스코드 봇) · `dashboard`(웹 대시보드) · `tool`(기타 도구) ·
  `client`(마인크래프트 클라이언트/런처)
- `url`: 디스코드 봇 초대 링크, 웹사이트, 또는 공개 레포. 스팸성 링크는 거절된다.
- `verified`: 운영자가 실제 트래픽과 철칙 준수를 확인하면 `true`로 바꾼다.
  신규 등록은 언제나 `false`로 시작한다.

## 등록된 통합

| 이름 | 유형 | 소개 | 링크 |
|---|---|---|---|
| — | — | 아직 첫 번째 등록을 기다리는 중이다. 주인공이 되어보자. | — |

## 등록이 거절되는 경우

- 원장 밖에 자체 잔액 필드를 둔 흔적이 보이는 경우
- 과도한 폴링(5초 미만)이나 `429` 무시
- 허위 링크·스팸·운영진 사칭

## 문의

등록 자격·레이트리밋 상향·키 발급은 풀문 네트워크 디스코드 운영진에게.
