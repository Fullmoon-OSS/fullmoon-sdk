# 통합 카탈로그 (INTEGRATIONS)

> 이 SDK·API 위에 만들어진 봇·대시보드·도구의 공식 목록이에요. 풀문 생태계의
> "플러그인 마켓"이에요. 자기 화폐를 만들지 않고 원장을 같이 읽는 프로젝트라면
> 누구나 PR 한 장으로 등록할 수 있어요.
>
> 카탈로그는 **[market.fullmoon.ink](https://market.fullmoon.ink)** 사이트에서
> 실시간으로 서빙돼요. 이 레포의 `registry/integrations.json`이 데이터 원본이에요.

## 등록 자격 — 3가지 철칙을 지켜주세요

등록 자체는 자유지만, 유지되려면 아래를 지켜야 해요. 운영자는 등록된 통합이
이 규칙을 깨면 `verified`를 빼거나 목록에서 내릴 수 있어요.

1. **잔액을 캐시하지 않아요** — 표시 직전에 조회해요.
2. **자체 화폐/잔액 테이블을 만들지 않아요** — 읽기 API만 써요. 쓰기 엔드포인트는
   존재하지 않아요(405).
3. **숫자를 하드코딩하지 않아요** — 배수·캡은 `/v1/config`에서 읽어요.

매너 항목: 폴링 주기는 5초 이상으로 해요, `429`를 받으면 백오프해요, 트래픽
폭주는 금지예요.

## 등록 절차

1. 이 레포를 포크해요.
2. [`registry/integrations.json`](./registry/integrations.json)에 항목을 추가해요.
   스키마는 아래와 같아요.
3. `npm run validate:registry`가 통과하는지 확인해요. CI도 같은 검증을 돌려요.
4. PR을 보내요. 운영자가 확인 후 병합해요. 병합되면 마켓 사이트에 바로 반영돼요.

### 항목 스키마

```json
{
  "name": "내 봇 이름",
  "type": "bot | dashboard | tool | client",
  "description": "한 줄 소개 — 무엇을 보여주는지 (40자 내외)",
  "url": "https://초대링크-또는-사이트-또는-레포",
  "author": "제작자 핸들",
  "addedAt": "2026-09-08",
  "verified": false
}
```

- `type`: `bot`(디스코드 봇) · `dashboard`(웹 대시보드) · `tool`(기타 도구) ·
  `client`(마인크래프트 클라이언트/런처) 중 하나예요.
- `url`: 디스코드 봇 초대 링크, 웹사이트, 또는 공개 레포예요. 스팸성 링크는
  거절돼요.
- `verified`: 운영자가 실제 트래픽과 철칙 준수를 확인하면 `true`로 바꿔요.
  신규 등록은 언제나 `false`로 시작해요.

## 등록된 통합

| 이름 | 유형 | 소개 | 링크 |
|---|---|---|---|
| — | — | 아직 첫 번째 등록을 기다리고 있어요. 주인공이 되어보세요! | — |

## 등록이 거절되는 경우

- 원장 밖에 자체 잔액 필드를 둔 흔적이 보이는 경우예요
- 과도한 폴링(5초 미만)이나 `429` 무시예요
- 허위 링크·스팸·운영진 사칭이에요

## 문의

등록 자격·레이트리밋 상향·키 발급은 풀문 네트워크 디스코드 운영진에게 문의해 주세요.
