# 커뮤니티 모듈 (MODULES)

> 이 SDK·API 위에 만들어진 봇·대시보드·도구의 공식 목록이에요. 풀문은 단일
> 서버 커뮤니티라서 마켓플레이스가 아니라 **커뮤니티 모듈**이 더 정확한
> 이름이에요. 자기 화폐를 만들지 않고 원장을 같이 읽는 프로젝트라면 누구나
> PR 한 장으로 등록할 수 있어요.
>
> 카탈로그는 **[modules.fullmoon.ink](https://modules.fullmoon.ink)** 사이트에서
> 실시간으로 서빙돼요. 이 레포의 `registry/modules.json`이 데이터 원본이에요.

## 등록 자격 — 3가지 철칙을 지켜주세요

등록 자체는 자유지만, 유지되려면 아래를 지켜야 해요. 운영자는 등록된 모듈이
이 규칙을 깨면 `verified`를 빼거나 목록에서 내릴 수 있어요.

1. **잔액을 캐시하지 않아요** — 표시 직전에 조회해요.
2. **자체 화폐/잔액 테이블을 만들지 않아요** — 읽기 API만 써요. 쓰기 엔드포인트는
   존재하지 않아요(405).
3. **숫자를 하드코딩하지 않아요** — 배수·캡은 `/v1/config`에서 읽어요.

매너 항목: 폴링 주기는 5초 이상으로 해요, `429`를 받으면 백오프해요, 트래픽
폭주는 금지예요.

## 등록 절차

git이 편한 개발자는 PR로, 아니면 **이슈 한 장**으로도 등록할 수 있어요.
기여 자체가 진입장벽이 되지 않는 게 이 카탈로그의 원칙이에요 — 번거로운 쪽은
언제나 운영자가 감수해요.

### 방법 A — 이슈 한 장 (git 불필요)

[모듈 등록 이슈](https://github.com/fullmoon-network/fullmoon-sdk/issues/new?template=module-registration.yml)를
열어 이름·유형·소개·링크를 적어 제출하면 끝이에요. 운영자가 확인해서
`registry/modules.json`에 대신 넣어주고, 병합되면 모듈 사이트에 바로 반영돼요.

### 방법 B — 직접 PR

1. 이 레포를 포크해요.
2. [`registry/modules.json`](./registry/modules.json)에 항목을 추가해요.
   스키마는 아래와 같아요.
3. 로컬에 Node가 있으면 `npm run validate:registry`를 돌려보세요. 없어도 괜찮아요
   — CI가 PR에서 같은 검증을 돌려줘요.
4. PR을 보내요. 운영자가 확인 후 병합해요. 병합되면 모듈 사이트에 바로 반영돼요.

### 항목 스키마

```json
{
  "name": "내 봇 이름",
  "type": "bot | dashboard | tool | client",
  "description": "한 줄 소개 — 무엇을 보여주는지 (40자 내외)",
  "url": "https://초대링크-또는-사이트-또는-레포",
  "source": "https://github.com/you/your-module",
  "author": "제작자 핸들",
  "addedAt": "2026-09-08",
  "verified": false
}
```

- `type`: `bot`(디스코드 봇) · `dashboard`(웹 대시보드) · `tool`(기타 도구) ·
  `client`(마인크래프트 클라이언트/런처) 중 하나예요.
- `url`: 디스코드 봇 초대 링크, 웹사이트, 또는 공개 레포예요. 스팸성 링크는
  거절돼요.
- `verified`: 운영자가 실제 트래픽과 철칙 준수를 확인하면 `true`로 바꿔요.
  신규 등록은 언제나 `false`로 시작해요.
- `source`: 모듈 코드의 공개 레포예요. 필수는 아니지만 봇/도구라면 강력 권장 — 운영자가 병합 전에 심사해요.

## 등록된 모듈

| 이름 | 유형 | 소개 | 링크 |
|---|---|---|---|
| — | — | 아직 첫 번째 등록을 기다리고 있어요. 주인공이 되어보세요! | — |

## 등록이 거절되는 경우

- 원장 밖에 자체 잔액 필드를 둔 흔적이 보이는 경우예요
- 과도한 폴링(5초 미만)이나 `429` 무시예요
- 허위 링크·스팸·운영진 사칭이에요
- 소스 미공개 + 난독화 흔적, 또는 과도한 디스코드 권한 요구예요

## 운영자 심사 체크리스트 (병합 전)

- 소스 공개 — `source` 레포에 난독화·minified 덩어리·eval·원격 코드 로딩이 없는지
- 요구 권한 최소 — 디스코드 봇 초대 권한이 기능에 필요한 최소범위인지
- 3가지 철칙 준수 — 잔액 캐시·자체 화폐·하드코딩 흔적이 없는지
- url 실접속 — 초대 링크·사이트가 실제로 그 모듈인지
- 키는 모듈별 개별 발급 (공유 금지, 폐기 가능)
- 실행 경계 — 모듈 코드는 개발자 인프라에서만 돌아간다 (policies.md「모듈 실행 경계」)


## 문의

등록 자격·레이트리밋 상향·키 발급은 풀문 네트워크 디스코드 운영진에게 문의해 주세요.
